<h2><%= gettext "New Break" %></h2>

<%= render "form.html", Map.put(assigns, :action, address_break_path(@conn, :create, @address.id)) %>

<span><%= link gettext("Cancel"), to: address_path(@conn, :show, @address) %></span>
